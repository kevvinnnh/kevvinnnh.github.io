import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://kevvinnnh.github.io',   // root domain for user repo
  output: 'static',                      // default; fine to be explicit
});
